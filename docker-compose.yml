version: "3.9"

services:
  mongo:
    image: mongo:8.0
    container_name: restcontrol-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: restcontrol
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
